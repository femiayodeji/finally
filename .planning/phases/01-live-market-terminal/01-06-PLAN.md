---
phase: 01-live-market-terminal
plan: 06
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - frontend/components/MainChartPanel.tsx
  - frontend/components/MainChart.tsx
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "Clicking a watchlist ticker shows a larger detail chart for that ticker"
    - "The detail chart renders already-populated (backfilled from /api/prices/{ticker}/history) then extends live from the SSE frame"
    - "On load the first watchlist ticker is auto-selected so the chart is never empty when a watchlist exists"
  artifacts:
    - frontend/components/MainChartPanel.tsx
    - frontend/components/MainChart.tsx
  key_links:
    - "MainChartPanel reads the selected ticker from SelectedTickerContext (set by watchlist row clicks in Plan 05)"
    - "MainChart consumes usePriceHistory(selected, useTickerPrice(selected)) so it backfills then extends via SSE without deriving series client-side"
    - "the header readout (symbol + live price + session_change_percent) reads the same SSE tick as the chart"
---

<objective>
Fill the main detail chart panel: a larger filled-area line chart for the currently-selected ticker that renders populated immediately (history backfill) and then extends live from the SSE stream, with the first watchlist ticker auto-selected on load and watchlist row clicks driving selection.

Purpose: Completes the phase's headline interaction — click a ticker, see its live detail chart (UI-02, PLAN.md §10, CONTEXT D-01/D-02, discretion: filled-area line + auto-select first ticker).
Output: MainChartPanel (panel + header readout + empty/loading states) and MainChart (Lightweight Charts area series).
</objective>

<execution_context>
@/home/femiayodeji/sandbox/andela/augmented-engineering/finally/.claude/gsd-core/workflows/execute-plan.md
@/home/femiayodeji/sandbox/andela/augmented-engineering/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-live-market-terminal/01-UI-SPEC.md
@frontend/lib/PriceStreamContext.tsx
@frontend/lib/usePriceHistory.ts
@frontend/lib/WatchlistContext.tsx
@frontend/lib/format.ts
@frontend/lib/types.ts
@frontend/lib/SelectedTickerContext.tsx
@frontend/components/Panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: MainChart — filled-area Lightweight Charts series with backfill + live extend</name>
  <read_first>
    - .planning/phases/01-live-market-terminal/01-UI-SPEC.md ("Main chart panel": filled-area line, line color accent-blue, area gradient rgba(32,157,215,0.35)→transparent, transparent background inheriting bg-canvas, faint/low-opacity or omitted gridlines, axis text neutral 12px Label, crosshair on hover, brief "Loading chart…" backfill state)
    - frontend/lib/usePriceHistory.ts (usePriceHistory(ticker, latest) → PricePoint[]; already backfills from /api/prices/{ticker}/history then extends as SSE ticks arrive — consume it, do not reimplement)
    - frontend/lib/PriceStreamContext.tsx (useTickerPrice(ticker) → PriceUpdate | undefined; SSE is the live source)
    - frontend/lib/types.ts (PricePoint {timestamp, price}; PriceUpdate fields incl. session_change_percent)
  </read_first>
  <files>frontend/components/MainChart.tsx</files>
  <action>
    Create `MainChart.tsx`, a `"use client"` component taking a `points: PricePoint[]` prop (and optionally the ticker for keying). Instantiate a TradingView Lightweight Charts chart once in a ref with an area series (filled-area line): line color `accent-blue` (#209dd7), top area color `rgba(32,157,215,0.35)` fading to transparent at the bottom, transparent chart background (inherits bg-canvas), gridlines faint at low opacity or omitted, axis/time-scale text in `neutral` (#8b949e) at Label size, crosshair enabled on hover. Map `points` to the series data shape (time from PricePoint.timestamp, value from price) and `setData` whenever points change so the chart renders populated on first paint and grows in place as new points append; resize to its container (ResizeObserver or the chart's autosize) and dispose the chart on unmount. Do not derive or smooth values — plot exactly the backend-provided series plus streamed points (UI-10 semantics; UI-10 itself is owned by Plan 05, do not re-list it).
  </action>
  <verify>
    <automated>cd frontend && npm run build && test -f out/index.html</automated>
  </verify>
  <done>MainChart renders a populated filled-area line from a points array and extends in place as points grow, styled per the UI-SPEC; disposes cleanly on unmount; build passes.</done>
  <acceptance_criteria>
    - area series uses accent-blue line + the specified area gradient over a transparent background
    - the chart renders populated on first paint given non-empty points (never empty-then-appearing)
    - new appended points extend the chart in place without a full re-mount
    - chart is created once in a ref and disposed on unmount (no leaked instances)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: MainChartPanel — selection wiring, auto-select first ticker, header readout, states</name>
  <read_first>
    - .planning/phases/01-live-market-terminal/01-UI-SPEC.md ("Main chart panel" header row: selected symbol Heading 18/600 + live price Display 28/600 tabular-nums flashing on update + session_change_percent pill badge; auto-selects first watchlist ticker on load; empty state "No chart to show" / "Add a ticker to your watchlist to see its chart.")
    - frontend/lib/SelectedTickerContext.tsx (created by Plan 03 — { selected, setSelected }; watchlist rows in Plan 05 call setSelected)
    - frontend/lib/WatchlistContext.tsx (useWatchlist().entries — source for the first-ticker auto-select)
    - frontend/lib/PriceStreamContext.tsx (useTickerPrice for the live header readout) + frontend/lib/format.ts (formatCurrency, formatPercent)
    - frontend/components/Panel.tsx and the Plan 03 placeholder MainChartPanel.tsx being replaced
  </read_first>
  <files>frontend/components/MainChartPanel.tsx</files>
  <action>
    Replace the Plan 03 placeholder `MainChartPanel.tsx`. Read `selected` from `SelectedTickerContext`; when `selected` is null and `useWatchlist().entries` is non-empty, auto-select the first entry's ticker via `setSelected` (effect guarded so it only fires when nothing is selected yet — CONTEXT discretion: auto-select first watchlist ticker on load). Compute the series with `usePriceHistory(selected, useTickerPrice(selected))` and pass it to `<MainChart>`. Render the panel header row per the UI-SPEC: selected symbol (Heading 18/600), live price (Display 28/600, tabular-nums, flashing background on tick using the same 500ms fade mechanics as the watchlist rows, honoring prefers-reduced-motion) and a `session_change_percent` pill badge (Label 12px, positive/negative low-opacity background). Show a brief centered "Loading chart…" (neutral) while the backfill series is still empty for a freshly-selected ticker, and the empty state ("No chart to show" / "Add a ticker to your watchlist to see its chart.") only when the watchlist itself is empty. Wrap everything in `<Panel>` matching the shell's panel treatment. Selection changes (from watchlist row clicks) must swap the chart to the newly-selected ticker, which re-backfills then extends live.
  </action>
  <verify>
    <automated>cd frontend && npm run build && test -f out/index.html</automated>
  </verify>
  <done>The main chart panel auto-selects the first ticker on load, swaps on row click, backfills-then-extends the detail chart, and shows the live symbol/price/change-% header with the correct loading/empty states; build passes.</done>
  <acceptance_criteria>
    - first watchlist ticker is auto-selected on load (chart never empty when a watchlist exists)
    - clicking a watchlist row switches the chart to that ticker (re-backfill then live extend)
    - header shows selected symbol + live flashing price + session_change_percent badge from the same SSE tick
    - "Loading chart…" shows briefly during backfill; the empty state shows only when the watchlist is empty
  </acceptance_criteria>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this plan produces
- `frontend/components/MainChart.tsx` — TradingView Lightweight Charts filled-area line series; backfilled + live-extended via usePriceHistory
- `frontend/components/MainChartPanel.tsx` — selection wiring (SelectedTickerContext), first-ticker auto-select, live symbol/price/session-change-% header readout, loading/empty states (replaces the Plan 03 placeholder)
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST/SSE data → chart DOM | history-backfill points and streamed ticks rendered into the canvas chart |
| selected-ticker state → API | the selected ticker becomes a /api/prices/{ticker}/history path param |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Denial of Service | Lightweight Charts main instance | medium | mitigate | Create one chart in a ref and dispose on unmount/ticker-swap; feed the already-bounded usePriceHistory series (≤600 pts) so chart memory is capped |
| T-06-02 | Tampering | selected ticker → history path param | low | mitigate | Selection is drawn only from validated watchlist entries (Plan 04 validated on add); the backend uppercases + returns an empty series for unknown tickers, never an injection surface |
| T-06-03 | Injection (XSS) | symbol/price header text | low | mitigate | Rendered as React text nodes (auto-escaped); no dangerouslySetInnerHTML |
| T-06-SC | Tampering | package installs | low | accept | No new packages — lightweight-charts was installed and human-verified in Plan 03's legitimacy checkpoint |
</threat_model>

<verification>
- `cd frontend && npm run build && test -f out/index.html` — export builds
- Manual (full stack): open the app → the first watchlist ticker's detail chart renders populated immediately and extends live; the header shows its symbol, live flashing price, and session change %; clicking another watchlist row swaps the chart (brief "Loading chart…" then populated + live); emptying the watchlist shows "No chart to show"
</verification>

<success_criteria>
Clicking a ticker (or loading with a watchlist present) shows a larger filled-area detail chart that renders populated immediately from history and extends live from SSE, with a live symbol/price/change-% header and correct loading/empty states.
</success_criteria>

<output>
Create `.planning/phases/01-live-market-terminal/01-06-SUMMARY.md` when done.
</output>
