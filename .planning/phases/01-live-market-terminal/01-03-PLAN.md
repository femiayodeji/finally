---
phase: 01-live-market-terminal
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/tsconfig.json
  - frontend/next.config.ts
  - frontend/postcss.config.mjs
  - frontend/tailwind.config.ts
  - frontend/app/globals.css
  - frontend/app/layout.tsx
  - frontend/app/page.tsx
  - frontend/components/Panel.tsx
  - frontend/components/Header.tsx
  - frontend/components/ConnectionDot.tsx
  - frontend/components/WatchlistPanel.tsx
  - frontend/components/MainChartPanel.tsx
  - frontend/components/PlaceholderPanel.tsx
  - frontend/lib/SelectedTickerContext.tsx
autonomous: false
requirements: [UI-09, APP-02]
user_setup: []
must_haves:
  truths:
    - "npm run build produces a static export in frontend/out with output:'export'"
    - "Opening the app shows the dark Bloomberg-style multi-panel grid (header, watchlist column, main chart area, placeholder panels) using the locked theme tokens"
    - "The header connection dot is live — green/yellow/red driven by PriceStreamContext.status"
    - "Dev mode proxies /api/* to the FastAPI backend so the SSE stream and REST calls reach it"
  artifacts:
    - frontend/next.config.ts
    - frontend/tailwind.config.ts
    - frontend/app/layout.tsx
    - frontend/app/page.tsx
    - frontend/components/ConnectionDot.tsx
    - frontend/lib/SelectedTickerContext.tsx
  key_links:
    - "layout.tsx mounts PriceStreamProvider and WatchlistProvider from frontend/lib so every panel reads the same SSE frame and watchlist"
    - "ConnectionDot reads usePriceStream().status"
    - "tailwind color tokens include the names frontend/lib/format.ts already references: up, down, ink-dim"
---

<objective>
Scaffold the Next.js + TypeScript static-export app (Tailwind dark theme, dev `/api` proxy) and build the terminal shell: the full Bloomberg-style grid with a live header connection dot and placeholder panels for later phases (D-05/D-06). This is the frontend half of the walking skeleton — open the app, the shell renders, the SSE context connects, the dot goes green.

Purpose: Establishes the frontend build/theme/layout foundation every UI slice fills in, without touching the locked `frontend/lib/*` contracts.
Output: Next.js project config + `app/` shell + shared components + a selected-ticker context.
</objective>

<execution_context>
@/home/femiayodeji/sandbox/andela/augmented-engineering/finally/.claude/gsd-core/workflows/execute-plan.md
@/home/femiayodeji/sandbox/andela/augmented-engineering/finally/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@planning/PLAN.md
@.planning/phases/01-live-market-terminal/01-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@frontend/lib/PriceStreamContext.tsx
@frontend/lib/WatchlistContext.tsx
@frontend/lib/api.ts
@frontend/lib/format.ts
@frontend/lib/types.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Nothing yet — this gate precedes the first `npm install`. No RESEARCH.md Package Legitimacy Audit exists for this phase, so per the package-legitimacy fallback policy all frontend packages are treated as [ASSUMED] and must be human-verified before install.</what-built>
  <how-to-verify>
    Confirm each package below is the legitimate, widely-used package on npmjs.com (check publisher, weekly downloads, repository link) before the executor runs `npm install`:
    1. `next` — https://www.npmjs.com/package/next (Vercel)
    2. `react` and `react-dom` — https://www.npmjs.com/package/react (Meta)
    3. `typescript` + `@types/react` + `@types/react-dom` + `@types/node`
    4. `tailwindcss` + `postcss` + `autoprefixer` — https://www.npmjs.com/package/tailwindcss
    5. `lightweight-charts` — https://www.npmjs.com/package/lightweight-charts (TradingView; used by Plans 05 & 06 per D-01)
    Pin explicit versions in package.json (no wildcard ranges) and prefer versions already resolved in the stale `frontend/node_modules` where compatible.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with install, or name any package to replace/pin differently.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Next.js TypeScript scaffold with static export, Tailwind dark theme, and dev API proxy</name>
  <read_first>
    - .planning/phases/01-live-market-terminal/01-UI-SPEC.md (Design System, Spacing Scale, Typography, Color table + "Suggested Tailwind token names"; Inter font; tabular-nums for numerics)
    - frontend/lib/format.ts (directionColorClass returns the Tailwind classes text-up / text-down / text-ink-dim — the config MUST define color tokens named up, down, ink-dim or these classes break)
    - frontend/lib/api.ts (BASE = "/api"; comment: proxied in dev via next.config.ts, served by FastAPI in prod)
    - planning/PLAN.md §3/§10/§11 (static export served by FastAPI on :8000, same-origin /api/*, Tailwind dark theme)
    - CONTEXT.md D-09 (frontend not yet scaffolded; stale .next/node_modules/out present — scaffold fresh; do not modify frontend/lib contracts)
  </read_first>
  <files>frontend/package.json, frontend/tsconfig.json, frontend/next.config.ts, frontend/postcss.config.mjs, frontend/tailwind.config.ts, frontend/app/globals.css</files>
  <action>
    Scaffold a Next.js App-Router TypeScript project in `frontend/` (keep the existing `frontend/lib/*` untouched). `package.json`: scripts dev/build/start/lint, pinned deps approved in the checkpoint. `next.config.ts`: set `output: 'export'` (static export to `out/`), `images.unoptimized: true` (export requirement), and `async rewrites()` proxying `/api/:path*` → `http://localhost:8000/api/:path*` for local dev (the export ignores rewrites in prod, where FastAPI serves same-origin — that's intended). `tsconfig.json`: strict, `moduleResolution: bundler`, path alias `@/*` → project root so `@/lib/...` and `@/components/...` resolve. Tailwind: `postcss.config.mjs` with tailwindcss + autoprefixer; `tailwind.config.ts` `content` globs covering `app/**` and `components/**` and `lib/**`; extend `colors` with the LOCKED tokens — `canvas #0d1117`, `panel #1a1a2e`, `border-muted #30363d`, `accent-blue #209dd7`, `accent-yellow #ecad0a`, `accent-purple #753991` (reserved, unused this phase), `positive #3fb950`, `negative #f85149`, `neutral #8b949e`, AND aliases `up #3fb950`, `down #f85149`, `ink-dim #8b949e` so the existing `text-up`/`text-down`/`text-ink-dim` classes from lib/format.ts resolve. `globals.css`: Tailwind directives, set body background to canvas and default text color, load Inter, and apply `font-variant-numeric: tabular-nums` to a utility used by numeric readouts. Confirm `npm run build` emits `frontend/out/`.
  </action>
  <verify>
    <automated>cd frontend && npm run build && test -f out/index.html</automated>
  </verify>
  <done>`npm run build` produces a static export at frontend/out with output:'export'; Tailwind tokens (including up/down/ink-dim) are defined; dev rewrites proxy /api/* to :8000.</done>
  <acceptance_criteria>
    - next.config.ts sets output:'export' and rewrites /api/:path* to the backend for dev
    - tailwind.config.ts defines all UI-SPEC tokens plus up/down/ink-dim aliases used by lib/format.ts
    - path alias @/* resolves; frontend/lib/* left unmodified
    - build output exists at frontend/out/index.html
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Root layout, providers, terminal grid shell, live connection dot, placeholder panels</name>
  <read_first>
    - .planning/phases/01-live-market-terminal/01-UI-SPEC.md ("Phase 1 Surface Notes": layout shell ASCII, Header spec, Connection status dot spec, Placeholder panel copy table)
    - frontend/lib/PriceStreamContext.tsx (PriceStreamProvider, usePriceStream().status = connected/reconnecting/disconnected)
    - frontend/lib/WatchlistContext.tsx (WatchlistProvider — mount near root so the watchlist panel in Plan 05 has state)
    - frontend/lib/types.ts (Direction, ConnectionStatus source of truth)
  </read_first>
  <files>frontend/app/layout.tsx, frontend/app/page.tsx, frontend/components/Panel.tsx, frontend/components/Header.tsx, frontend/components/ConnectionDot.tsx, frontend/components/WatchlistPanel.tsx, frontend/components/MainChartPanel.tsx, frontend/components/PlaceholderPanel.tsx, frontend/lib/SelectedTickerContext.tsx</files>
  <action>
    `app/layout.tsx`: root layout (dark, Inter) wrapping children in `PriceStreamProvider` → `WatchlistProvider` → a new `SelectedTickerProvider` (created below). `lib/SelectedTickerContext.tsx`: a small `"use client"` context holding `{ selected: string | null, setSelected }` so Plan 05 (row click sets it) and Plan 06 (chart reads it) share selection WITHOUT editing each other's files — this is the seam that lets those two plans run in parallel. `components/Panel.tsx`: reusable panel wrapper (`bg-panel`, 1px `border-muted`, 6px radius, 16px padding) with an optional title. `components/ConnectionDot.tsx`: reads `usePriceStream().status`, renders an 8px CSS circle — green (connected, static), accent-yellow (reconnecting, 1.5s opacity pulse), red (disconnected) — plus the text label per UI-SPEC. `components/Header.tsx`: 56px bar, "FinAlly" wordmark in accent-yellow, then the ConnectionDot+label, then TOTAL VALUE / CASH stub captions rendering a literal em-dash "—" in neutral (D-06 — never $0.00). `components/PlaceholderPanel.tsx`: centered Heading(neutral)+Body(neutral) empty-state, driven by props, used for Positions/Heatmap/P&L/AI-Assistant with the exact copy from the UI-SPEC Placeholder table (the AI Assistant panel shows a grayed, non-focusable input preview). `components/WatchlistPanel.tsx` and `components/MainChartPanel.tsx`: create as thin PLACEHOLDER components now (render inside a Panel with a "streaming…" / empty state) — Plans 05 and 06 replace their bodies. `app/page.tsx`: compose the grid per the UI-SPEC ASCII — header on top; left 320px watchlist column (`<WatchlistPanel/>`); center column with `<MainChartPanel/>` (top ~60%) over a 3-up placeholder row (Heatmap | Positions | P&L); right 340px collapsible AI-chat placeholder rail. Desktop-first (≥1280px) with the tablet stack behavior noted in the UI-SPEC. Respect the color/spacing/typography tokens exactly.
  </action>
  <verify>
    <automated>cd frontend && npm run build && test -f out/index.html</automated>
  </verify>
  <done>The exported app renders the dark multi-panel terminal shell with a live connection dot and correctly-copied placeholder panels; WatchlistPanel/MainChartPanel exist as replaceable placeholders; providers are mounted at the root.</done>
  <acceptance_criteria>
    - layout mounts PriceStreamProvider, WatchlistProvider, SelectedTickerProvider at the root
    - ConnectionDot reflects usePriceStream().status with the three locked colors/states
    - header shows the yellow wordmark and em-dash TOTAL VALUE / CASH stubs (never $0.00)
    - all four placeholder panels use the exact UI-SPEC copy; AI panel's preview input is non-interactive
    - WatchlistPanel and MainChartPanel exist as standalone files ready to be filled by Plans 05/06
    - build succeeds
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | third-party packages pulled into the frontend build |
| browser → /api proxy | dev rewrites forward client calls to the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (next, react, tailwind, lightweight-charts, tooling) | high | mitigate | Blocking human-verify checkpoint before install (no RESEARCH audit exists → all packages [ASSUMED]); pin explicit versions on npmjs.com-verified publishers; no wildcard ranges |
| T-03-01 | Information Disclosure | next.config rewrites | low | accept | The `/api/*` dev proxy targets only localhost:8000; the static prod export contains no proxy and is same-origin under FastAPI |
| T-03-02 | Spoofing | connection-status dot | low | mitigate | Dot derives solely from the real EventSource lifecycle (PriceStreamContext.status), so it cannot show "connected" without an open stream |
</threat_model>

<verification>
- `cd frontend && npm run build && test -f out/index.html` — static export builds
- Manual (full stack): backend running on :8000, `cd frontend && npm run dev`, open the app → dark shell renders, connection dot turns green once SSE frames arrive, placeholder panels show the locked copy
</verification>

<success_criteria>
Next.js static-export app scaffolded with the locked dark theme and dev /api proxy; terminal grid shell renders with a live connection dot and correctly-worded placeholder panels; frontend/lib contracts untouched; providers mounted at root.
</success_criteria>

<output>
Create `.planning/phases/01-live-market-terminal/01-03-SUMMARY.md` when done.
</output>
